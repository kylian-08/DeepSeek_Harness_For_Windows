# electron-builder configuration for DSH4Win
# Build:  powershell -ExecutionPolicy Bypass -File scripts/build.ps1
appId: com.dshharness.app
productName: DSH4Win
copyright: Copyright (c) 2026

directories:
  output: dist
  buildResources: build-resources

# The asar contains only the Electron shell. The dsh application ships as raw
# files under resources/runtime (copied by the afterPack hook) so native
# modules and profile junctions keep real filesystem paths.
files:
  - electron/main.js
  - assets/icon.ico
  - package.json
  - "!node_modules/**/*"

afterPack: scripts/after-pack.js

npmRebuild: false
nodeGypRebuild: false

win:
  target:
    - target: nsis
      arch:
        - x64
  icon: assets/icon.ico
  artifactName: DSH4Win-Setup-${version}.${ext}

nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: DSH4Win
  uninstallDisplayName: DSH4Win (DeepSeek Harness for Windows)
